import type { DeviceNotBrowser, TransactionForSim } from './types';
import type { Device } from '@web3-onboard/common';
/**
 * Takes in TransactionRequest and converts all Hex values to numbers
 * @param transaction
 * @returns a transaction where all Hex properties are now numbers
 */
export declare const hexFieldsToNumber: (transaction: TransactionForSim) => TransactionRequestWithNumberFields;
type TransactionRequestWithNumberFields = TransactionForSim & {
    gas: number;
    value: number;
    gasPrice?: number;
    gasLimit?: number;
    maxPriorityFeePerGas?: number;
    maxFeePerGas?: number;
};
export declare function getDevice(): Device | DeviceNotBrowser;
export {};
